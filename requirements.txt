# Pinned direct dependencies for TagExplorer 1.0.1 Windows release builds.
# Regenerate a full transitive hash lock from requirements.in on the release
# machine before publishing artifacts.
PyQt6==6.11.0
opencv-python==4.13.0.92
PyInstaller==6.20.0
sqlcipher3-wheels==0.5.7
pywinstyles==1.8
